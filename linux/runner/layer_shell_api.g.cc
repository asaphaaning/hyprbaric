// Autogenerated from Pigeon (v29.0.1), do not edit directly.
// See also: https://pub.dev/packages/pigeon

#include <cmath>

#include <string.h>
#include "layer_shell_api.g.h"
static guint G_GNUC_UNUSED flpigeon_hash_double(double v) {
  if (std::isnan(v)) {
    return static_cast<guint>(0x7FF80000);
  }
  if (v == 0.0) {
    v = 0.0;
  }
  union { double d; uint64_t u; } u;
  u.d = v;
  return static_cast<guint>(u.u ^ (u.u >> 32));
}
static gboolean G_GNUC_UNUSED flpigeon_equals_double(double a, double b) {
  return (a == b) || (std::isnan(a) && std::isnan(b));
}
static gboolean G_GNUC_UNUSED flpigeon_deep_equals(FlValue* a, FlValue* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (fl_value_get_type(a) != fl_value_get_type(b)) {
    return FALSE;
  }
  switch (fl_value_get_type(a)) {
    case FL_VALUE_TYPE_NULL:
      return TRUE;
    case FL_VALUE_TYPE_BOOL:
      return fl_value_get_bool(a) == fl_value_get_bool(b);
    case FL_VALUE_TYPE_INT:
      return fl_value_get_int(a) == fl_value_get_int(b);
    case FL_VALUE_TYPE_FLOAT: {
      return flpigeon_equals_double(fl_value_get_float(a), fl_value_get_float(b));
    }
    case FL_VALUE_TYPE_STRING:
      return g_strcmp0(fl_value_get_string(a), fl_value_get_string(b)) == 0;
    case FL_VALUE_TYPE_UINT8_LIST:
      return fl_value_get_length(a) == fl_value_get_length(b) &&
             memcmp(fl_value_get_uint8_list(a), fl_value_get_uint8_list(b), fl_value_get_length(a)) == 0;
    case FL_VALUE_TYPE_INT32_LIST:
      return fl_value_get_length(a) == fl_value_get_length(b) &&
             memcmp(fl_value_get_int32_list(a), fl_value_get_int32_list(b), fl_value_get_length(a) * sizeof(int32_t)) == 0;
    case FL_VALUE_TYPE_INT64_LIST:
      return fl_value_get_length(a) == fl_value_get_length(b) &&
             memcmp(fl_value_get_int64_list(a), fl_value_get_int64_list(b), fl_value_get_length(a) * sizeof(int64_t)) == 0;
    case FL_VALUE_TYPE_FLOAT_LIST: {
      size_t len = fl_value_get_length(a);
      if (len != fl_value_get_length(b)) {
        return FALSE;
      }
      const double* a_data = fl_value_get_float_list(a);
      const double* b_data = fl_value_get_float_list(b);
      for (size_t i = 0; i < len; i++) {
      if (!flpigeon_equals_double(a_data[i], b_data[i])) {
        return FALSE;
      }
    }
      return TRUE;
    }
    case FL_VALUE_TYPE_LIST: {
      size_t len = fl_value_get_length(a);
      if (len != fl_value_get_length(b)) {
        return FALSE;
      }
      for (size_t i = 0; i < len; i++) {
      if (!flpigeon_deep_equals(fl_value_get_list_value(a, i), fl_value_get_list_value(b, i))) {
        return FALSE;
      }
    }
      return TRUE;
    }
    case FL_VALUE_TYPE_MAP: {
      size_t len = fl_value_get_length(a);
      if (len != fl_value_get_length(b)) {
        return FALSE;
      }
      for (size_t i = 0; i < len; i++) {
      FlValue* key = fl_value_get_map_key(a, i);
      FlValue* val = fl_value_get_map_value(a, i);
      gboolean found = FALSE;
      for (size_t j = 0; j < len; j++) {
        FlValue* b_key = fl_value_get_map_key(b, j);
        if (flpigeon_deep_equals(key, b_key)) {
          FlValue* b_val = fl_value_get_map_value(b, j);
          if (flpigeon_deep_equals(val, b_val)) {
            found = TRUE;
            break;
          } else {
            return FALSE;
          }
        }
      }
      if (!found) {
        return FALSE;
      }
    }
      return TRUE;
    }
    default:
      return FALSE;
  }
  return FALSE;
}
static guint G_GNUC_UNUSED flpigeon_deep_hash(FlValue* value) {
  if (value == nullptr) {
    return 0;
  }
  switch (fl_value_get_type(value)) {
    case FL_VALUE_TYPE_NULL:
      return 0;
    case FL_VALUE_TYPE_BOOL:
      return fl_value_get_bool(value) ? 1231 : 1237;
    case FL_VALUE_TYPE_INT: {
      int64_t v = fl_value_get_int(value);
      return static_cast<guint>(v ^ (v >> 32));
    }
    case FL_VALUE_TYPE_FLOAT:
      return flpigeon_hash_double(fl_value_get_float(value));
    case FL_VALUE_TYPE_STRING:
      return g_str_hash(fl_value_get_string(value));
    case FL_VALUE_TYPE_UINT8_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      const uint8_t* data = fl_value_get_uint8_list(value);
      for (size_t i = 0; i < len; i++) {
        result = result * 31 + data[i];
      }
      return result;
    }
    case FL_VALUE_TYPE_INT32_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      const int32_t* data = fl_value_get_int32_list(value);
      for (size_t i = 0; i < len; i++) {
        result = result * 31 + static_cast<guint>(data[i]);
      }
      return result;
    }
    case FL_VALUE_TYPE_INT64_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      const int64_t* data = fl_value_get_int64_list(value);
      for (size_t i = 0; i < len; i++) {
        result = result * 31 + static_cast<guint>(data[i] ^ (data[i] >> 32));
      }
      return result;
    }
    case FL_VALUE_TYPE_FLOAT_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      const double* data = fl_value_get_float_list(value);
      for (size_t i = 0; i < len; i++) {
      result = result * 31 + flpigeon_hash_double(data[i]);
    }
      return result;
    }
    case FL_VALUE_TYPE_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      for (size_t i = 0; i < len; i++) {
      result = result * 31 + flpigeon_deep_hash(fl_value_get_list_value(value, i));
    }
      return result;
    }
    case FL_VALUE_TYPE_MAP: {
      guint result = 0;
      size_t len = fl_value_get_length(value);
      for (size_t i = 0; i < len; i++) {
      result += ((flpigeon_deep_hash(fl_value_get_map_key(value, i)) * 31) ^ flpigeon_deep_hash(fl_value_get_map_value(value, i)));
    }
      return result;
    }
    default:
      return static_cast<guint>(fl_value_get_type(value));
  }
  return 0;
}
static gchar* G_GNUC_UNUSED flpigeon_to_string(FlValue* value) {
  if (value == nullptr) {
    return g_strdup("null");
  }
  switch (fl_value_get_type(value)) {
    case FL_VALUE_TYPE_NULL:
      return g_strdup("null");
    case FL_VALUE_TYPE_BOOL:
      return g_strdup(fl_value_get_bool(value) ? "true" : "false");
    case FL_VALUE_TYPE_INT:
      return g_strdup_printf("%" G_GINT64_FORMAT, fl_value_get_int(value));
    case FL_VALUE_TYPE_FLOAT:
      return g_strdup_printf("%g", fl_value_get_float(value));
    case FL_VALUE_TYPE_STRING:
      return g_strdup_printf("\"%s\"", fl_value_get_string(value));
    case FL_VALUE_TYPE_UINT8_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      const uint8_t* data = fl_value_get_uint8_list(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        g_string_append_printf(str, "%d", data[i]);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_INT32_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      const int32_t* data = fl_value_get_int32_list(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        g_string_append_printf(str, "%d", data[i]);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_INT64_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      const int64_t* data = fl_value_get_int64_list(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        g_string_append_printf(str, "%" G_GINT64_FORMAT, data[i]);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_FLOAT_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      const double* data = fl_value_get_float_list(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        g_string_append_printf(str, "%g", data[i]);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        gchar* item_str = flpigeon_to_string(fl_value_get_list_value(value, i));
        g_string_append(str, item_str);
        g_free(item_str);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_MAP: {
      GString* str = g_string_new("{");
      size_t len = fl_value_get_length(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        gchar* key_str = flpigeon_to_string(fl_value_get_map_key(value, i));
        gchar* val_str = flpigeon_to_string(fl_value_get_map_value(value, i));
        g_string_append_printf(str, "%s: %s", key_str, val_str);
        g_free(key_str);
        g_free(val_str);
      }
      g_string_append(str, "}");
      return g_string_free(str, FALSE);
    }
    default:
      return g_strdup("[custom]");
  }
  return g_strdup("null");
}


struct _HyprbaricNativeLayerShellMonitorTarget {
  GObject parent_instance;

  HyprbaricNativeLayerShellMonitorTargetKind kind;
  gchar* name;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellMonitorTarget, hyprbaric_native_layer_shell_monitor_target, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_monitor_target_dispose(GObject* object) {
  HyprbaricNativeLayerShellMonitorTarget* self = HYPRBARIC_NATIVE_LAYER_SHELL_MONITOR_TARGET(object);
  g_clear_pointer(&self->name, g_free);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_monitor_target_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_monitor_target_init(HyprbaricNativeLayerShellMonitorTarget* self) {
}

static void hyprbaric_native_layer_shell_monitor_target_class_init(HyprbaricNativeLayerShellMonitorTargetClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_monitor_target_dispose;
}

HyprbaricNativeLayerShellMonitorTarget* hyprbaric_native_layer_shell_monitor_target_new(HyprbaricNativeLayerShellMonitorTargetKind kind, const gchar* name) {
  HyprbaricNativeLayerShellMonitorTarget* self = HYPRBARIC_NATIVE_LAYER_SHELL_MONITOR_TARGET(g_object_new(hyprbaric_native_layer_shell_monitor_target_get_type(), nullptr));
  self->kind = kind;
  if (name != nullptr) {
    self->name = g_strdup(name);
  }
  else {
    self->name = nullptr;
  }
  return self;
}

HyprbaricNativeLayerShellMonitorTargetKind hyprbaric_native_layer_shell_monitor_target_get_kind(HyprbaricNativeLayerShellMonitorTarget* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR_TARGET(self), static_cast<HyprbaricNativeLayerShellMonitorTargetKind>(0));
  return self->kind;
}

const gchar* hyprbaric_native_layer_shell_monitor_target_get_name(HyprbaricNativeLayerShellMonitorTarget* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR_TARGET(self), nullptr);
  return self->name;
}

static FlValue* hyprbaric_native_layer_shell_monitor_target_to_list(HyprbaricNativeLayerShellMonitorTarget* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_custom(hyprbaric_native_layer_shell_monitor_target_kind_type_id, fl_value_new_int(self->kind), (GDestroyNotify)fl_value_unref));
  fl_value_append_take(values, self->name != nullptr ? fl_value_new_string(self->name) : fl_value_new_null());
  return values;
}

static HyprbaricNativeLayerShellMonitorTarget* hyprbaric_native_layer_shell_monitor_target_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  HyprbaricNativeLayerShellMonitorTargetKind kind = static_cast<HyprbaricNativeLayerShellMonitorTargetKind>(fl_value_get_int(reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value0)))));
  FlValue* value1 = fl_value_get_list_value(values, 1);
  const gchar* name = nullptr;
  if (fl_value_get_type(value1) != FL_VALUE_TYPE_NULL) {
    name = fl_value_get_string(value1);
  }
  return hyprbaric_native_layer_shell_monitor_target_new(kind, name);
}

gboolean hyprbaric_native_layer_shell_monitor_target_equals(HyprbaricNativeLayerShellMonitorTarget* a, HyprbaricNativeLayerShellMonitorTarget* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (a->kind != b->kind) {
    return FALSE;
  }
  if (g_strcmp0(a->name, b->name) != 0) {
    return FALSE;
  }
  return TRUE;
}

guint hyprbaric_native_layer_shell_monitor_target_hash(HyprbaricNativeLayerShellMonitorTarget* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR_TARGET(self), 0);
  guint result = 0;
  result = result * 31 + static_cast<guint>(self->kind);
  result = result * 31 + (self->name != nullptr ? g_str_hash(self->name) : 0);
  return result;
}

gchar* hyprbaric_native_layer_shell_monitor_target_to_string(HyprbaricNativeLayerShellMonitorTarget* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR_TARGET(self), NULL);
  GString* str = g_string_new("NativeLayerShellMonitorTarget(");
  g_string_append(str, "kind: ");
  g_string_append_printf(str, "%d", static_cast<int>(self->kind));
  g_string_append(str, ", name: ");
  if (self->name != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->name);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _HyprbaricNativeLayerShellMonitor {
  GObject parent_instance;

  gchar* name;
  gchar* label;
  gboolean is_primary;
  int64_t x;
  int64_t y;
  int64_t width;
  int64_t height;
  int64_t refresh_rate_millihertz;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellMonitor, hyprbaric_native_layer_shell_monitor, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_monitor_dispose(GObject* object) {
  HyprbaricNativeLayerShellMonitor* self = HYPRBARIC_NATIVE_LAYER_SHELL_MONITOR(object);
  g_clear_pointer(&self->name, g_free);
  g_clear_pointer(&self->label, g_free);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_monitor_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_monitor_init(HyprbaricNativeLayerShellMonitor* self) {
}

static void hyprbaric_native_layer_shell_monitor_class_init(HyprbaricNativeLayerShellMonitorClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_monitor_dispose;
}

HyprbaricNativeLayerShellMonitor* hyprbaric_native_layer_shell_monitor_new(const gchar* name, const gchar* label, gboolean is_primary, int64_t x, int64_t y, int64_t width, int64_t height, int64_t refresh_rate_millihertz) {
  HyprbaricNativeLayerShellMonitor* self = HYPRBARIC_NATIVE_LAYER_SHELL_MONITOR(g_object_new(hyprbaric_native_layer_shell_monitor_get_type(), nullptr));
  self->name = g_strdup(name);
  self->label = g_strdup(label);
  self->is_primary = is_primary;
  self->x = x;
  self->y = y;
  self->width = width;
  self->height = height;
  self->refresh_rate_millihertz = refresh_rate_millihertz;
  return self;
}

const gchar* hyprbaric_native_layer_shell_monitor_get_name(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), nullptr);
  return self->name;
}

const gchar* hyprbaric_native_layer_shell_monitor_get_label(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), nullptr);
  return self->label;
}

gboolean hyprbaric_native_layer_shell_monitor_get_is_primary(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), FALSE);
  return self->is_primary;
}

int64_t hyprbaric_native_layer_shell_monitor_get_x(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), 0);
  return self->x;
}

int64_t hyprbaric_native_layer_shell_monitor_get_y(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), 0);
  return self->y;
}

int64_t hyprbaric_native_layer_shell_monitor_get_width(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), 0);
  return self->width;
}

int64_t hyprbaric_native_layer_shell_monitor_get_height(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), 0);
  return self->height;
}

int64_t hyprbaric_native_layer_shell_monitor_get_refresh_rate_millihertz(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), 0);
  return self->refresh_rate_millihertz;
}

static FlValue* hyprbaric_native_layer_shell_monitor_to_list(HyprbaricNativeLayerShellMonitor* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_string(self->name));
  fl_value_append_take(values, fl_value_new_string(self->label));
  fl_value_append_take(values, fl_value_new_bool(self->is_primary));
  fl_value_append_take(values, fl_value_new_int(self->x));
  fl_value_append_take(values, fl_value_new_int(self->y));
  fl_value_append_take(values, fl_value_new_int(self->width));
  fl_value_append_take(values, fl_value_new_int(self->height));
  fl_value_append_take(values, fl_value_new_int(self->refresh_rate_millihertz));
  return values;
}

static HyprbaricNativeLayerShellMonitor* hyprbaric_native_layer_shell_monitor_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  const gchar* name = fl_value_get_string(value0);
  FlValue* value1 = fl_value_get_list_value(values, 1);
  const gchar* label = fl_value_get_string(value1);
  FlValue* value2 = fl_value_get_list_value(values, 2);
  gboolean is_primary = fl_value_get_bool(value2);
  FlValue* value3 = fl_value_get_list_value(values, 3);
  int64_t x = fl_value_get_int(value3);
  FlValue* value4 = fl_value_get_list_value(values, 4);
  int64_t y = fl_value_get_int(value4);
  FlValue* value5 = fl_value_get_list_value(values, 5);
  int64_t width = fl_value_get_int(value5);
  FlValue* value6 = fl_value_get_list_value(values, 6);
  int64_t height = fl_value_get_int(value6);
  FlValue* value7 = fl_value_get_list_value(values, 7);
  int64_t refresh_rate_millihertz = fl_value_get_int(value7);
  return hyprbaric_native_layer_shell_monitor_new(name, label, is_primary, x, y, width, height, refresh_rate_millihertz);
}

gboolean hyprbaric_native_layer_shell_monitor_equals(HyprbaricNativeLayerShellMonitor* a, HyprbaricNativeLayerShellMonitor* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (g_strcmp0(a->name, b->name) != 0) {
    return FALSE;
  }
  if (g_strcmp0(a->label, b->label) != 0) {
    return FALSE;
  }
  if (a->is_primary != b->is_primary) {
    return FALSE;
  }
  if (a->x != b->x) {
    return FALSE;
  }
  if (a->y != b->y) {
    return FALSE;
  }
  if (a->width != b->width) {
    return FALSE;
  }
  if (a->height != b->height) {
    return FALSE;
  }
  if (a->refresh_rate_millihertz != b->refresh_rate_millihertz) {
    return FALSE;
  }
  return TRUE;
}

guint hyprbaric_native_layer_shell_monitor_hash(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), 0);
  guint result = 0;
  result = result * 31 + (self->name != nullptr ? g_str_hash(self->name) : 0);
  result = result * 31 + (self->label != nullptr ? g_str_hash(self->label) : 0);
  result = result * 31 + static_cast<guint>(self->is_primary);
  result = result * 31 + static_cast<guint>(self->x);
  result = result * 31 + static_cast<guint>(self->y);
  result = result * 31 + static_cast<guint>(self->width);
  result = result * 31 + static_cast<guint>(self->height);
  result = result * 31 + static_cast<guint>(self->refresh_rate_millihertz);
  return result;
}

gchar* hyprbaric_native_layer_shell_monitor_to_string(HyprbaricNativeLayerShellMonitor* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MONITOR(self), NULL);
  GString* str = g_string_new("NativeLayerShellMonitor(");
  g_string_append(str, "name: ");
  if (self->name != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->name);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", label: ");
  if (self->label != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->label);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", is_primary: ");
  g_string_append(str, self->is_primary ? "true" : "false");
  g_string_append(str, ", x: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->x);
  g_string_append(str, ", y: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->y);
  g_string_append(str, ", width: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->width);
  g_string_append(str, ", height: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->height);
  g_string_append(str, ", refresh_rate_millihertz: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->refresh_rate_millihertz);
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _HyprbaricNativeLayerShellAnchors {
  GObject parent_instance;

  gboolean* top;
  gboolean* bottom;
  gboolean* left;
  gboolean* right;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellAnchors, hyprbaric_native_layer_shell_anchors, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_anchors_dispose(GObject* object) {
  HyprbaricNativeLayerShellAnchors* self = HYPRBARIC_NATIVE_LAYER_SHELL_ANCHORS(object);
  g_clear_pointer(&self->top, g_free);
  g_clear_pointer(&self->bottom, g_free);
  g_clear_pointer(&self->left, g_free);
  g_clear_pointer(&self->right, g_free);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_anchors_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_anchors_init(HyprbaricNativeLayerShellAnchors* self) {
}

static void hyprbaric_native_layer_shell_anchors_class_init(HyprbaricNativeLayerShellAnchorsClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_anchors_dispose;
}

HyprbaricNativeLayerShellAnchors* hyprbaric_native_layer_shell_anchors_new(gboolean* top, gboolean* bottom, gboolean* left, gboolean* right) {
  HyprbaricNativeLayerShellAnchors* self = HYPRBARIC_NATIVE_LAYER_SHELL_ANCHORS(g_object_new(hyprbaric_native_layer_shell_anchors_get_type(), nullptr));
  if (top != nullptr) {
    self->top = static_cast<gboolean*>(malloc(sizeof(gboolean)));
    *self->top = *top;
  }
  else {
    self->top = nullptr;
  }
  if (bottom != nullptr) {
    self->bottom = static_cast<gboolean*>(malloc(sizeof(gboolean)));
    *self->bottom = *bottom;
  }
  else {
    self->bottom = nullptr;
  }
  if (left != nullptr) {
    self->left = static_cast<gboolean*>(malloc(sizeof(gboolean)));
    *self->left = *left;
  }
  else {
    self->left = nullptr;
  }
  if (right != nullptr) {
    self->right = static_cast<gboolean*>(malloc(sizeof(gboolean)));
    *self->right = *right;
  }
  else {
    self->right = nullptr;
  }
  return self;
}

gboolean* hyprbaric_native_layer_shell_anchors_get_top(HyprbaricNativeLayerShellAnchors* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_ANCHORS(self), nullptr);
  return self->top;
}

gboolean* hyprbaric_native_layer_shell_anchors_get_bottom(HyprbaricNativeLayerShellAnchors* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_ANCHORS(self), nullptr);
  return self->bottom;
}

gboolean* hyprbaric_native_layer_shell_anchors_get_left(HyprbaricNativeLayerShellAnchors* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_ANCHORS(self), nullptr);
  return self->left;
}

gboolean* hyprbaric_native_layer_shell_anchors_get_right(HyprbaricNativeLayerShellAnchors* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_ANCHORS(self), nullptr);
  return self->right;
}

static FlValue* hyprbaric_native_layer_shell_anchors_to_list(HyprbaricNativeLayerShellAnchors* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, self->top != nullptr ? fl_value_new_bool(*self->top) : fl_value_new_null());
  fl_value_append_take(values, self->bottom != nullptr ? fl_value_new_bool(*self->bottom) : fl_value_new_null());
  fl_value_append_take(values, self->left != nullptr ? fl_value_new_bool(*self->left) : fl_value_new_null());
  fl_value_append_take(values, self->right != nullptr ? fl_value_new_bool(*self->right) : fl_value_new_null());
  return values;
}

static HyprbaricNativeLayerShellAnchors* hyprbaric_native_layer_shell_anchors_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  gboolean* top = nullptr;
  gboolean top_value;
  if (fl_value_get_type(value0) != FL_VALUE_TYPE_NULL) {
    top_value = fl_value_get_bool(value0);
    top = &top_value;
  }
  FlValue* value1 = fl_value_get_list_value(values, 1);
  gboolean* bottom = nullptr;
  gboolean bottom_value;
  if (fl_value_get_type(value1) != FL_VALUE_TYPE_NULL) {
    bottom_value = fl_value_get_bool(value1);
    bottom = &bottom_value;
  }
  FlValue* value2 = fl_value_get_list_value(values, 2);
  gboolean* left = nullptr;
  gboolean left_value;
  if (fl_value_get_type(value2) != FL_VALUE_TYPE_NULL) {
    left_value = fl_value_get_bool(value2);
    left = &left_value;
  }
  FlValue* value3 = fl_value_get_list_value(values, 3);
  gboolean* right = nullptr;
  gboolean right_value;
  if (fl_value_get_type(value3) != FL_VALUE_TYPE_NULL) {
    right_value = fl_value_get_bool(value3);
    right = &right_value;
  }
  return hyprbaric_native_layer_shell_anchors_new(top, bottom, left, right);
}

gboolean hyprbaric_native_layer_shell_anchors_equals(HyprbaricNativeLayerShellAnchors* a, HyprbaricNativeLayerShellAnchors* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if ((a->top == nullptr) != (b->top == nullptr)) {
    return FALSE;
  }
  if (a->top != nullptr && *a->top != *b->top) {
    return FALSE;
  }
  if ((a->bottom == nullptr) != (b->bottom == nullptr)) {
    return FALSE;
  }
  if (a->bottom != nullptr && *a->bottom != *b->bottom) {
    return FALSE;
  }
  if ((a->left == nullptr) != (b->left == nullptr)) {
    return FALSE;
  }
  if (a->left != nullptr && *a->left != *b->left) {
    return FALSE;
  }
  if ((a->right == nullptr) != (b->right == nullptr)) {
    return FALSE;
  }
  if (a->right != nullptr && *a->right != *b->right) {
    return FALSE;
  }
  return TRUE;
}

guint hyprbaric_native_layer_shell_anchors_hash(HyprbaricNativeLayerShellAnchors* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_ANCHORS(self), 0);
  guint result = 0;
  result = result * 31 + (self->top != nullptr ? static_cast<guint>(*self->top) : 0);
  result = result * 31 + (self->bottom != nullptr ? static_cast<guint>(*self->bottom) : 0);
  result = result * 31 + (self->left != nullptr ? static_cast<guint>(*self->left) : 0);
  result = result * 31 + (self->right != nullptr ? static_cast<guint>(*self->right) : 0);
  return result;
}

gchar* hyprbaric_native_layer_shell_anchors_to_string(HyprbaricNativeLayerShellAnchors* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_ANCHORS(self), NULL);
  GString* str = g_string_new("NativeLayerShellAnchors(");
  g_string_append(str, "top: ");
  if (self->top != nullptr) {
    g_string_append(str, *self->top ? "true" : "false");
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", bottom: ");
  if (self->bottom != nullptr) {
    g_string_append(str, *self->bottom ? "true" : "false");
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", left: ");
  if (self->left != nullptr) {
    g_string_append(str, *self->left ? "true" : "false");
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", right: ");
  if (self->right != nullptr) {
    g_string_append(str, *self->right ? "true" : "false");
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _HyprbaricNativeLayerShellMargins {
  GObject parent_instance;

  int64_t* top;
  int64_t* bottom;
  int64_t* left;
  int64_t* right;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellMargins, hyprbaric_native_layer_shell_margins, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_margins_dispose(GObject* object) {
  HyprbaricNativeLayerShellMargins* self = HYPRBARIC_NATIVE_LAYER_SHELL_MARGINS(object);
  g_clear_pointer(&self->top, g_free);
  g_clear_pointer(&self->bottom, g_free);
  g_clear_pointer(&self->left, g_free);
  g_clear_pointer(&self->right, g_free);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_margins_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_margins_init(HyprbaricNativeLayerShellMargins* self) {
}

static void hyprbaric_native_layer_shell_margins_class_init(HyprbaricNativeLayerShellMarginsClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_margins_dispose;
}

HyprbaricNativeLayerShellMargins* hyprbaric_native_layer_shell_margins_new(int64_t* top, int64_t* bottom, int64_t* left, int64_t* right) {
  HyprbaricNativeLayerShellMargins* self = HYPRBARIC_NATIVE_LAYER_SHELL_MARGINS(g_object_new(hyprbaric_native_layer_shell_margins_get_type(), nullptr));
  if (top != nullptr) {
    self->top = static_cast<int64_t*>(malloc(sizeof(int64_t)));
    *self->top = *top;
  }
  else {
    self->top = nullptr;
  }
  if (bottom != nullptr) {
    self->bottom = static_cast<int64_t*>(malloc(sizeof(int64_t)));
    *self->bottom = *bottom;
  }
  else {
    self->bottom = nullptr;
  }
  if (left != nullptr) {
    self->left = static_cast<int64_t*>(malloc(sizeof(int64_t)));
    *self->left = *left;
  }
  else {
    self->left = nullptr;
  }
  if (right != nullptr) {
    self->right = static_cast<int64_t*>(malloc(sizeof(int64_t)));
    *self->right = *right;
  }
  else {
    self->right = nullptr;
  }
  return self;
}

int64_t* hyprbaric_native_layer_shell_margins_get_top(HyprbaricNativeLayerShellMargins* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MARGINS(self), nullptr);
  return self->top;
}

int64_t* hyprbaric_native_layer_shell_margins_get_bottom(HyprbaricNativeLayerShellMargins* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MARGINS(self), nullptr);
  return self->bottom;
}

int64_t* hyprbaric_native_layer_shell_margins_get_left(HyprbaricNativeLayerShellMargins* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MARGINS(self), nullptr);
  return self->left;
}

int64_t* hyprbaric_native_layer_shell_margins_get_right(HyprbaricNativeLayerShellMargins* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MARGINS(self), nullptr);
  return self->right;
}

static FlValue* hyprbaric_native_layer_shell_margins_to_list(HyprbaricNativeLayerShellMargins* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, self->top != nullptr ? fl_value_new_int(*self->top) : fl_value_new_null());
  fl_value_append_take(values, self->bottom != nullptr ? fl_value_new_int(*self->bottom) : fl_value_new_null());
  fl_value_append_take(values, self->left != nullptr ? fl_value_new_int(*self->left) : fl_value_new_null());
  fl_value_append_take(values, self->right != nullptr ? fl_value_new_int(*self->right) : fl_value_new_null());
  return values;
}

static HyprbaricNativeLayerShellMargins* hyprbaric_native_layer_shell_margins_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  int64_t* top = nullptr;
  int64_t top_value;
  if (fl_value_get_type(value0) != FL_VALUE_TYPE_NULL) {
    top_value = fl_value_get_int(value0);
    top = &top_value;
  }
  FlValue* value1 = fl_value_get_list_value(values, 1);
  int64_t* bottom = nullptr;
  int64_t bottom_value;
  if (fl_value_get_type(value1) != FL_VALUE_TYPE_NULL) {
    bottom_value = fl_value_get_int(value1);
    bottom = &bottom_value;
  }
  FlValue* value2 = fl_value_get_list_value(values, 2);
  int64_t* left = nullptr;
  int64_t left_value;
  if (fl_value_get_type(value2) != FL_VALUE_TYPE_NULL) {
    left_value = fl_value_get_int(value2);
    left = &left_value;
  }
  FlValue* value3 = fl_value_get_list_value(values, 3);
  int64_t* right = nullptr;
  int64_t right_value;
  if (fl_value_get_type(value3) != FL_VALUE_TYPE_NULL) {
    right_value = fl_value_get_int(value3);
    right = &right_value;
  }
  return hyprbaric_native_layer_shell_margins_new(top, bottom, left, right);
}

gboolean hyprbaric_native_layer_shell_margins_equals(HyprbaricNativeLayerShellMargins* a, HyprbaricNativeLayerShellMargins* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if ((a->top == nullptr) != (b->top == nullptr)) {
    return FALSE;
  }
  if (a->top != nullptr && *a->top != *b->top) {
    return FALSE;
  }
  if ((a->bottom == nullptr) != (b->bottom == nullptr)) {
    return FALSE;
  }
  if (a->bottom != nullptr && *a->bottom != *b->bottom) {
    return FALSE;
  }
  if ((a->left == nullptr) != (b->left == nullptr)) {
    return FALSE;
  }
  if (a->left != nullptr && *a->left != *b->left) {
    return FALSE;
  }
  if ((a->right == nullptr) != (b->right == nullptr)) {
    return FALSE;
  }
  if (a->right != nullptr && *a->right != *b->right) {
    return FALSE;
  }
  return TRUE;
}

guint hyprbaric_native_layer_shell_margins_hash(HyprbaricNativeLayerShellMargins* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MARGINS(self), 0);
  guint result = 0;
  result = result * 31 + (self->top != nullptr ? static_cast<guint>(*self->top) : 0);
  result = result * 31 + (self->bottom != nullptr ? static_cast<guint>(*self->bottom) : 0);
  result = result * 31 + (self->left != nullptr ? static_cast<guint>(*self->left) : 0);
  result = result * 31 + (self->right != nullptr ? static_cast<guint>(*self->right) : 0);
  return result;
}

gchar* hyprbaric_native_layer_shell_margins_to_string(HyprbaricNativeLayerShellMargins* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_MARGINS(self), NULL);
  GString* str = g_string_new("NativeLayerShellMargins(");
  g_string_append(str, "top: ");
  if (self->top != nullptr) {
    g_string_append_printf(str, "%" G_GINT64_FORMAT, *self->top);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", bottom: ");
  if (self->bottom != nullptr) {
    g_string_append_printf(str, "%" G_GINT64_FORMAT, *self->bottom);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", left: ");
  if (self->left != nullptr) {
    g_string_append_printf(str, "%" G_GINT64_FORMAT, *self->left);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", right: ");
  if (self->right != nullptr) {
    g_string_append_printf(str, "%" G_GINT64_FORMAT, *self->right);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _HyprbaricNativeLayerShellSize {
  GObject parent_instance;

  int64_t* width;
  int64_t* height;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellSize, hyprbaric_native_layer_shell_size, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_size_dispose(GObject* object) {
  HyprbaricNativeLayerShellSize* self = HYPRBARIC_NATIVE_LAYER_SHELL_SIZE(object);
  g_clear_pointer(&self->width, g_free);
  g_clear_pointer(&self->height, g_free);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_size_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_size_init(HyprbaricNativeLayerShellSize* self) {
}

static void hyprbaric_native_layer_shell_size_class_init(HyprbaricNativeLayerShellSizeClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_size_dispose;
}

HyprbaricNativeLayerShellSize* hyprbaric_native_layer_shell_size_new(int64_t* width, int64_t* height) {
  HyprbaricNativeLayerShellSize* self = HYPRBARIC_NATIVE_LAYER_SHELL_SIZE(g_object_new(hyprbaric_native_layer_shell_size_get_type(), nullptr));
  if (width != nullptr) {
    self->width = static_cast<int64_t*>(malloc(sizeof(int64_t)));
    *self->width = *width;
  }
  else {
    self->width = nullptr;
  }
  if (height != nullptr) {
    self->height = static_cast<int64_t*>(malloc(sizeof(int64_t)));
    *self->height = *height;
  }
  else {
    self->height = nullptr;
  }
  return self;
}

int64_t* hyprbaric_native_layer_shell_size_get_width(HyprbaricNativeLayerShellSize* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_SIZE(self), nullptr);
  return self->width;
}

int64_t* hyprbaric_native_layer_shell_size_get_height(HyprbaricNativeLayerShellSize* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_SIZE(self), nullptr);
  return self->height;
}

static FlValue* hyprbaric_native_layer_shell_size_to_list(HyprbaricNativeLayerShellSize* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, self->width != nullptr ? fl_value_new_int(*self->width) : fl_value_new_null());
  fl_value_append_take(values, self->height != nullptr ? fl_value_new_int(*self->height) : fl_value_new_null());
  return values;
}

static HyprbaricNativeLayerShellSize* hyprbaric_native_layer_shell_size_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  int64_t* width = nullptr;
  int64_t width_value;
  if (fl_value_get_type(value0) != FL_VALUE_TYPE_NULL) {
    width_value = fl_value_get_int(value0);
    width = &width_value;
  }
  FlValue* value1 = fl_value_get_list_value(values, 1);
  int64_t* height = nullptr;
  int64_t height_value;
  if (fl_value_get_type(value1) != FL_VALUE_TYPE_NULL) {
    height_value = fl_value_get_int(value1);
    height = &height_value;
  }
  return hyprbaric_native_layer_shell_size_new(width, height);
}

gboolean hyprbaric_native_layer_shell_size_equals(HyprbaricNativeLayerShellSize* a, HyprbaricNativeLayerShellSize* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if ((a->width == nullptr) != (b->width == nullptr)) {
    return FALSE;
  }
  if (a->width != nullptr && *a->width != *b->width) {
    return FALSE;
  }
  if ((a->height == nullptr) != (b->height == nullptr)) {
    return FALSE;
  }
  if (a->height != nullptr && *a->height != *b->height) {
    return FALSE;
  }
  return TRUE;
}

guint hyprbaric_native_layer_shell_size_hash(HyprbaricNativeLayerShellSize* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_SIZE(self), 0);
  guint result = 0;
  result = result * 31 + (self->width != nullptr ? static_cast<guint>(*self->width) : 0);
  result = result * 31 + (self->height != nullptr ? static_cast<guint>(*self->height) : 0);
  return result;
}

gchar* hyprbaric_native_layer_shell_size_to_string(HyprbaricNativeLayerShellSize* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_SIZE(self), NULL);
  GString* str = g_string_new("NativeLayerShellSize(");
  g_string_append(str, "width: ");
  if (self->width != nullptr) {
    g_string_append_printf(str, "%" G_GINT64_FORMAT, *self->width);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", height: ");
  if (self->height != nullptr) {
    g_string_append_printf(str, "%" G_GINT64_FORMAT, *self->height);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _HyprbaricNativeLayerShellRegion {
  GObject parent_instance;

  int64_t x;
  int64_t y;
  int64_t width;
  int64_t height;
  int64_t radius_top_left;
  int64_t radius_top_right;
  int64_t radius_bottom_right;
  int64_t radius_bottom_left;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellRegion, hyprbaric_native_layer_shell_region, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_region_dispose(GObject* object) {
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_region_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_region_init(HyprbaricNativeLayerShellRegion* self) {
}

static void hyprbaric_native_layer_shell_region_class_init(HyprbaricNativeLayerShellRegionClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_region_dispose;
}

HyprbaricNativeLayerShellRegion* hyprbaric_native_layer_shell_region_new(int64_t x, int64_t y, int64_t width, int64_t height, int64_t radius_top_left, int64_t radius_top_right, int64_t radius_bottom_right, int64_t radius_bottom_left) {
  HyprbaricNativeLayerShellRegion* self = HYPRBARIC_NATIVE_LAYER_SHELL_REGION(g_object_new(hyprbaric_native_layer_shell_region_get_type(), nullptr));
  self->x = x;
  self->y = y;
  self->width = width;
  self->height = height;
  self->radius_top_left = radius_top_left;
  self->radius_top_right = radius_top_right;
  self->radius_bottom_right = radius_bottom_right;
  self->radius_bottom_left = radius_bottom_left;
  return self;
}

int64_t hyprbaric_native_layer_shell_region_get_x(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), 0);
  return self->x;
}

int64_t hyprbaric_native_layer_shell_region_get_y(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), 0);
  return self->y;
}

int64_t hyprbaric_native_layer_shell_region_get_width(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), 0);
  return self->width;
}

int64_t hyprbaric_native_layer_shell_region_get_height(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), 0);
  return self->height;
}

int64_t hyprbaric_native_layer_shell_region_get_radius_top_left(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), 0);
  return self->radius_top_left;
}

int64_t hyprbaric_native_layer_shell_region_get_radius_top_right(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), 0);
  return self->radius_top_right;
}

int64_t hyprbaric_native_layer_shell_region_get_radius_bottom_right(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), 0);
  return self->radius_bottom_right;
}

int64_t hyprbaric_native_layer_shell_region_get_radius_bottom_left(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), 0);
  return self->radius_bottom_left;
}

static FlValue* hyprbaric_native_layer_shell_region_to_list(HyprbaricNativeLayerShellRegion* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_int(self->x));
  fl_value_append_take(values, fl_value_new_int(self->y));
  fl_value_append_take(values, fl_value_new_int(self->width));
  fl_value_append_take(values, fl_value_new_int(self->height));
  fl_value_append_take(values, fl_value_new_int(self->radius_top_left));
  fl_value_append_take(values, fl_value_new_int(self->radius_top_right));
  fl_value_append_take(values, fl_value_new_int(self->radius_bottom_right));
  fl_value_append_take(values, fl_value_new_int(self->radius_bottom_left));
  return values;
}

static HyprbaricNativeLayerShellRegion* hyprbaric_native_layer_shell_region_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  int64_t x = fl_value_get_int(value0);
  FlValue* value1 = fl_value_get_list_value(values, 1);
  int64_t y = fl_value_get_int(value1);
  FlValue* value2 = fl_value_get_list_value(values, 2);
  int64_t width = fl_value_get_int(value2);
  FlValue* value3 = fl_value_get_list_value(values, 3);
  int64_t height = fl_value_get_int(value3);
  FlValue* value4 = fl_value_get_list_value(values, 4);
  int64_t radius_top_left = fl_value_get_int(value4);
  FlValue* value5 = fl_value_get_list_value(values, 5);
  int64_t radius_top_right = fl_value_get_int(value5);
  FlValue* value6 = fl_value_get_list_value(values, 6);
  int64_t radius_bottom_right = fl_value_get_int(value6);
  FlValue* value7 = fl_value_get_list_value(values, 7);
  int64_t radius_bottom_left = fl_value_get_int(value7);
  return hyprbaric_native_layer_shell_region_new(x, y, width, height, radius_top_left, radius_top_right, radius_bottom_right, radius_bottom_left);
}

gboolean hyprbaric_native_layer_shell_region_equals(HyprbaricNativeLayerShellRegion* a, HyprbaricNativeLayerShellRegion* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (a->x != b->x) {
    return FALSE;
  }
  if (a->y != b->y) {
    return FALSE;
  }
  if (a->width != b->width) {
    return FALSE;
  }
  if (a->height != b->height) {
    return FALSE;
  }
  if (a->radius_top_left != b->radius_top_left) {
    return FALSE;
  }
  if (a->radius_top_right != b->radius_top_right) {
    return FALSE;
  }
  if (a->radius_bottom_right != b->radius_bottom_right) {
    return FALSE;
  }
  if (a->radius_bottom_left != b->radius_bottom_left) {
    return FALSE;
  }
  return TRUE;
}

guint hyprbaric_native_layer_shell_region_hash(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), 0);
  guint result = 0;
  result = result * 31 + static_cast<guint>(self->x);
  result = result * 31 + static_cast<guint>(self->y);
  result = result * 31 + static_cast<guint>(self->width);
  result = result * 31 + static_cast<guint>(self->height);
  result = result * 31 + static_cast<guint>(self->radius_top_left);
  result = result * 31 + static_cast<guint>(self->radius_top_right);
  result = result * 31 + static_cast<guint>(self->radius_bottom_right);
  result = result * 31 + static_cast<guint>(self->radius_bottom_left);
  return result;
}

gchar* hyprbaric_native_layer_shell_region_to_string(HyprbaricNativeLayerShellRegion* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION(self), NULL);
  GString* str = g_string_new("NativeLayerShellRegion(");
  g_string_append(str, "x: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->x);
  g_string_append(str, ", y: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->y);
  g_string_append(str, ", width: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->width);
  g_string_append(str, ", height: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->height);
  g_string_append(str, ", radius_top_left: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->radius_top_left);
  g_string_append(str, ", radius_top_right: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->radius_top_right);
  g_string_append(str, ", radius_bottom_right: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->radius_bottom_right);
  g_string_append(str, ", radius_bottom_left: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->radius_bottom_left);
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _HyprbaricNativeLayerShellRegionRequest {
  GObject parent_instance;

  int64_t bar_height;
  HyprbaricNativeLayerShellBarEdge bar_edge;
  HyprbaricNativeLayerShellRegion* menu;
  FlValue* passive_regions;
  gboolean capture_all_clicks;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellRegionRequest, hyprbaric_native_layer_shell_region_request, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_region_request_dispose(GObject* object) {
  HyprbaricNativeLayerShellRegionRequest* self = HYPRBARIC_NATIVE_LAYER_SHELL_REGION_REQUEST(object);
  g_clear_object(&self->menu);
  g_clear_pointer(&self->passive_regions, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_region_request_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_region_request_init(HyprbaricNativeLayerShellRegionRequest* self) {
}

static void hyprbaric_native_layer_shell_region_request_class_init(HyprbaricNativeLayerShellRegionRequestClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_region_request_dispose;
}

HyprbaricNativeLayerShellRegionRequest* hyprbaric_native_layer_shell_region_request_new(int64_t bar_height, HyprbaricNativeLayerShellBarEdge bar_edge, HyprbaricNativeLayerShellRegion* menu, FlValue* passive_regions, gboolean capture_all_clicks) {
  HyprbaricNativeLayerShellRegionRequest* self = HYPRBARIC_NATIVE_LAYER_SHELL_REGION_REQUEST(g_object_new(hyprbaric_native_layer_shell_region_request_get_type(), nullptr));
  self->bar_height = bar_height;
  self->bar_edge = bar_edge;
  if (menu != nullptr) {
    self->menu = HYPRBARIC_NATIVE_LAYER_SHELL_REGION(g_object_ref(menu));
  }
  else {
    self->menu = nullptr;
  }
  self->passive_regions = fl_value_ref(passive_regions);
  self->capture_all_clicks = capture_all_clicks;
  return self;
}

int64_t hyprbaric_native_layer_shell_region_request_get_bar_height(HyprbaricNativeLayerShellRegionRequest* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION_REQUEST(self), 0);
  return self->bar_height;
}

HyprbaricNativeLayerShellBarEdge hyprbaric_native_layer_shell_region_request_get_bar_edge(HyprbaricNativeLayerShellRegionRequest* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION_REQUEST(self), static_cast<HyprbaricNativeLayerShellBarEdge>(0));
  return self->bar_edge;
}

HyprbaricNativeLayerShellRegion* hyprbaric_native_layer_shell_region_request_get_menu(HyprbaricNativeLayerShellRegionRequest* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION_REQUEST(self), nullptr);
  return self->menu;
}

FlValue* hyprbaric_native_layer_shell_region_request_get_passive_regions(HyprbaricNativeLayerShellRegionRequest* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION_REQUEST(self), nullptr);
  return self->passive_regions;
}

gboolean hyprbaric_native_layer_shell_region_request_get_capture_all_clicks(HyprbaricNativeLayerShellRegionRequest* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION_REQUEST(self), FALSE);
  return self->capture_all_clicks;
}

static FlValue* hyprbaric_native_layer_shell_region_request_to_list(HyprbaricNativeLayerShellRegionRequest* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_int(self->bar_height));
  fl_value_append_take(values, fl_value_new_custom(hyprbaric_native_layer_shell_bar_edge_type_id, fl_value_new_int(self->bar_edge), (GDestroyNotify)fl_value_unref));
  fl_value_append_take(values, self->menu != nullptr ? fl_value_new_custom_object(hyprbaric_native_layer_shell_region_type_id, G_OBJECT(self->menu)) : fl_value_new_null());
  fl_value_append_take(values, fl_value_ref(self->passive_regions));
  fl_value_append_take(values, fl_value_new_bool(self->capture_all_clicks));
  return values;
}

static HyprbaricNativeLayerShellRegionRequest* hyprbaric_native_layer_shell_region_request_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  int64_t bar_height = fl_value_get_int(value0);
  FlValue* value1 = fl_value_get_list_value(values, 1);
  HyprbaricNativeLayerShellBarEdge bar_edge = static_cast<HyprbaricNativeLayerShellBarEdge>(fl_value_get_int(reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value1)))));
  FlValue* value2 = fl_value_get_list_value(values, 2);
  HyprbaricNativeLayerShellRegion* menu = nullptr;
  if (fl_value_get_type(value2) != FL_VALUE_TYPE_NULL) {
    menu = HYPRBARIC_NATIVE_LAYER_SHELL_REGION(fl_value_get_custom_value_object(value2));
  }
  FlValue* value3 = fl_value_get_list_value(values, 3);
  FlValue* passive_regions = value3;
  FlValue* value4 = fl_value_get_list_value(values, 4);
  gboolean capture_all_clicks = fl_value_get_bool(value4);
  return hyprbaric_native_layer_shell_region_request_new(bar_height, bar_edge, menu, passive_regions, capture_all_clicks);
}

gboolean hyprbaric_native_layer_shell_region_request_equals(HyprbaricNativeLayerShellRegionRequest* a, HyprbaricNativeLayerShellRegionRequest* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (a->bar_height != b->bar_height) {
    return FALSE;
  }
  if (a->bar_edge != b->bar_edge) {
    return FALSE;
  }
  if (!hyprbaric_native_layer_shell_region_equals(a->menu, b->menu)) {
    return FALSE;
  }
  if (!flpigeon_deep_equals(a->passive_regions, b->passive_regions)) {
    return FALSE;
  }
  if (a->capture_all_clicks != b->capture_all_clicks) {
    return FALSE;
  }
  return TRUE;
}

guint hyprbaric_native_layer_shell_region_request_hash(HyprbaricNativeLayerShellRegionRequest* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION_REQUEST(self), 0);
  guint result = 0;
  result = result * 31 + static_cast<guint>(self->bar_height);
  result = result * 31 + static_cast<guint>(self->bar_edge);
  result = result * 31 + hyprbaric_native_layer_shell_region_hash(self->menu);
  result = result * 31 + flpigeon_deep_hash(self->passive_regions);
  result = result * 31 + static_cast<guint>(self->capture_all_clicks);
  return result;
}

gchar* hyprbaric_native_layer_shell_region_request_to_string(HyprbaricNativeLayerShellRegionRequest* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_REGION_REQUEST(self), NULL);
  GString* str = g_string_new("NativeLayerShellRegionRequest(");
  g_string_append(str, "bar_height: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->bar_height);
  g_string_append(str, ", bar_edge: ");
  g_string_append_printf(str, "%d", static_cast<int>(self->bar_edge));
  g_string_append(str, ", menu: ");
  if (self->menu != nullptr) {
    gchar* field_str = hyprbaric_native_layer_shell_region_to_string(self->menu);
    g_string_append(str, field_str);
    g_free(field_str);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", passive_regions: ");
  if (self->passive_regions != nullptr) {
    gchar* val_str = flpigeon_to_string(self->passive_regions);
    g_string_append(str, val_str);
    g_free(val_str);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", capture_all_clicks: ");
  g_string_append(str, self->capture_all_clicks ? "true" : "false");
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _HyprbaricNativeLayerShellPanelConfig {
  GObject parent_instance;

  gchar* app_namespace;
  HyprbaricNativeLayerShellLayer layer;
  HyprbaricNativeLayerShellAnchors* anchors;
  HyprbaricNativeLayerShellMargins* margins;
  HyprbaricNativeLayerShellSize* size;
  int64_t exclusive_zone;
  gboolean auto_exclusive_zone;
  HyprbaricNativeLayerShellKeyboardMode keyboard_mode;
  HyprbaricNativeLayerShellMonitorTarget* monitor;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellPanelConfig, hyprbaric_native_layer_shell_panel_config, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_panel_config_dispose(GObject* object) {
  HyprbaricNativeLayerShellPanelConfig* self = HYPRBARIC_NATIVE_LAYER_SHELL_PANEL_CONFIG(object);
  g_clear_pointer(&self->app_namespace, g_free);
  g_clear_object(&self->anchors);
  g_clear_object(&self->margins);
  g_clear_object(&self->size);
  g_clear_object(&self->monitor);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_panel_config_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_panel_config_init(HyprbaricNativeLayerShellPanelConfig* self) {
}

static void hyprbaric_native_layer_shell_panel_config_class_init(HyprbaricNativeLayerShellPanelConfigClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_panel_config_dispose;
}

HyprbaricNativeLayerShellPanelConfig* hyprbaric_native_layer_shell_panel_config_new(const gchar* app_namespace, HyprbaricNativeLayerShellLayer layer, HyprbaricNativeLayerShellAnchors* anchors, HyprbaricNativeLayerShellMargins* margins, HyprbaricNativeLayerShellSize* size, int64_t exclusive_zone, gboolean auto_exclusive_zone, HyprbaricNativeLayerShellKeyboardMode keyboard_mode, HyprbaricNativeLayerShellMonitorTarget* monitor) {
  HyprbaricNativeLayerShellPanelConfig* self = HYPRBARIC_NATIVE_LAYER_SHELL_PANEL_CONFIG(g_object_new(hyprbaric_native_layer_shell_panel_config_get_type(), nullptr));
  self->app_namespace = g_strdup(app_namespace);
  self->layer = layer;
  self->anchors = HYPRBARIC_NATIVE_LAYER_SHELL_ANCHORS(g_object_ref(anchors));
  self->margins = HYPRBARIC_NATIVE_LAYER_SHELL_MARGINS(g_object_ref(margins));
  self->size = HYPRBARIC_NATIVE_LAYER_SHELL_SIZE(g_object_ref(size));
  self->exclusive_zone = exclusive_zone;
  self->auto_exclusive_zone = auto_exclusive_zone;
  self->keyboard_mode = keyboard_mode;
  self->monitor = HYPRBARIC_NATIVE_LAYER_SHELL_MONITOR_TARGET(g_object_ref(monitor));
  return self;
}

const gchar* hyprbaric_native_layer_shell_panel_config_get_app_namespace(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), nullptr);
  return self->app_namespace;
}

HyprbaricNativeLayerShellLayer hyprbaric_native_layer_shell_panel_config_get_layer(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), static_cast<HyprbaricNativeLayerShellLayer>(0));
  return self->layer;
}

HyprbaricNativeLayerShellAnchors* hyprbaric_native_layer_shell_panel_config_get_anchors(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), nullptr);
  return self->anchors;
}

HyprbaricNativeLayerShellMargins* hyprbaric_native_layer_shell_panel_config_get_margins(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), nullptr);
  return self->margins;
}

HyprbaricNativeLayerShellSize* hyprbaric_native_layer_shell_panel_config_get_size(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), nullptr);
  return self->size;
}

int64_t hyprbaric_native_layer_shell_panel_config_get_exclusive_zone(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), 0);
  return self->exclusive_zone;
}

gboolean hyprbaric_native_layer_shell_panel_config_get_auto_exclusive_zone(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), FALSE);
  return self->auto_exclusive_zone;
}

HyprbaricNativeLayerShellKeyboardMode hyprbaric_native_layer_shell_panel_config_get_keyboard_mode(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), static_cast<HyprbaricNativeLayerShellKeyboardMode>(0));
  return self->keyboard_mode;
}

HyprbaricNativeLayerShellMonitorTarget* hyprbaric_native_layer_shell_panel_config_get_monitor(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), nullptr);
  return self->monitor;
}

static FlValue* hyprbaric_native_layer_shell_panel_config_to_list(HyprbaricNativeLayerShellPanelConfig* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_string(self->app_namespace));
  fl_value_append_take(values, fl_value_new_custom(hyprbaric_native_layer_shell_layer_type_id, fl_value_new_int(self->layer), (GDestroyNotify)fl_value_unref));
  fl_value_append_take(values, fl_value_new_custom_object(hyprbaric_native_layer_shell_anchors_type_id, G_OBJECT(self->anchors)));
  fl_value_append_take(values, fl_value_new_custom_object(hyprbaric_native_layer_shell_margins_type_id, G_OBJECT(self->margins)));
  fl_value_append_take(values, fl_value_new_custom_object(hyprbaric_native_layer_shell_size_type_id, G_OBJECT(self->size)));
  fl_value_append_take(values, fl_value_new_int(self->exclusive_zone));
  fl_value_append_take(values, fl_value_new_bool(self->auto_exclusive_zone));
  fl_value_append_take(values, fl_value_new_custom(hyprbaric_native_layer_shell_keyboard_mode_type_id, fl_value_new_int(self->keyboard_mode), (GDestroyNotify)fl_value_unref));
  fl_value_append_take(values, fl_value_new_custom_object(hyprbaric_native_layer_shell_monitor_target_type_id, G_OBJECT(self->monitor)));
  return values;
}

static HyprbaricNativeLayerShellPanelConfig* hyprbaric_native_layer_shell_panel_config_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  const gchar* app_namespace = fl_value_get_string(value0);
  FlValue* value1 = fl_value_get_list_value(values, 1);
  HyprbaricNativeLayerShellLayer layer = static_cast<HyprbaricNativeLayerShellLayer>(fl_value_get_int(reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value1)))));
  FlValue* value2 = fl_value_get_list_value(values, 2);
  HyprbaricNativeLayerShellAnchors* anchors = HYPRBARIC_NATIVE_LAYER_SHELL_ANCHORS(fl_value_get_custom_value_object(value2));
  FlValue* value3 = fl_value_get_list_value(values, 3);
  HyprbaricNativeLayerShellMargins* margins = HYPRBARIC_NATIVE_LAYER_SHELL_MARGINS(fl_value_get_custom_value_object(value3));
  FlValue* value4 = fl_value_get_list_value(values, 4);
  HyprbaricNativeLayerShellSize* size = HYPRBARIC_NATIVE_LAYER_SHELL_SIZE(fl_value_get_custom_value_object(value4));
  FlValue* value5 = fl_value_get_list_value(values, 5);
  int64_t exclusive_zone = fl_value_get_int(value5);
  FlValue* value6 = fl_value_get_list_value(values, 6);
  gboolean auto_exclusive_zone = fl_value_get_bool(value6);
  FlValue* value7 = fl_value_get_list_value(values, 7);
  HyprbaricNativeLayerShellKeyboardMode keyboard_mode = static_cast<HyprbaricNativeLayerShellKeyboardMode>(fl_value_get_int(reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value7)))));
  FlValue* value8 = fl_value_get_list_value(values, 8);
  HyprbaricNativeLayerShellMonitorTarget* monitor = HYPRBARIC_NATIVE_LAYER_SHELL_MONITOR_TARGET(fl_value_get_custom_value_object(value8));
  return hyprbaric_native_layer_shell_panel_config_new(app_namespace, layer, anchors, margins, size, exclusive_zone, auto_exclusive_zone, keyboard_mode, monitor);
}

gboolean hyprbaric_native_layer_shell_panel_config_equals(HyprbaricNativeLayerShellPanelConfig* a, HyprbaricNativeLayerShellPanelConfig* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (g_strcmp0(a->app_namespace, b->app_namespace) != 0) {
    return FALSE;
  }
  if (a->layer != b->layer) {
    return FALSE;
  }
  if (!hyprbaric_native_layer_shell_anchors_equals(a->anchors, b->anchors)) {
    return FALSE;
  }
  if (!hyprbaric_native_layer_shell_margins_equals(a->margins, b->margins)) {
    return FALSE;
  }
  if (!hyprbaric_native_layer_shell_size_equals(a->size, b->size)) {
    return FALSE;
  }
  if (a->exclusive_zone != b->exclusive_zone) {
    return FALSE;
  }
  if (a->auto_exclusive_zone != b->auto_exclusive_zone) {
    return FALSE;
  }
  if (a->keyboard_mode != b->keyboard_mode) {
    return FALSE;
  }
  if (!hyprbaric_native_layer_shell_monitor_target_equals(a->monitor, b->monitor)) {
    return FALSE;
  }
  return TRUE;
}

guint hyprbaric_native_layer_shell_panel_config_hash(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), 0);
  guint result = 0;
  result = result * 31 + (self->app_namespace != nullptr ? g_str_hash(self->app_namespace) : 0);
  result = result * 31 + static_cast<guint>(self->layer);
  result = result * 31 + hyprbaric_native_layer_shell_anchors_hash(self->anchors);
  result = result * 31 + hyprbaric_native_layer_shell_margins_hash(self->margins);
  result = result * 31 + hyprbaric_native_layer_shell_size_hash(self->size);
  result = result * 31 + static_cast<guint>(self->exclusive_zone);
  result = result * 31 + static_cast<guint>(self->auto_exclusive_zone);
  result = result * 31 + static_cast<guint>(self->keyboard_mode);
  result = result * 31 + hyprbaric_native_layer_shell_monitor_target_hash(self->monitor);
  return result;
}

gchar* hyprbaric_native_layer_shell_panel_config_to_string(HyprbaricNativeLayerShellPanelConfig* self) {
  g_return_val_if_fail(HYPRBARIC_IS_NATIVE_LAYER_SHELL_PANEL_CONFIG(self), NULL);
  GString* str = g_string_new("NativeLayerShellPanelConfig(");
  g_string_append(str, "app_namespace: ");
  if (self->app_namespace != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->app_namespace);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", layer: ");
  g_string_append_printf(str, "%d", static_cast<int>(self->layer));
  g_string_append(str, ", anchors: ");
  if (self->anchors != nullptr) {
    gchar* field_str = hyprbaric_native_layer_shell_anchors_to_string(self->anchors);
    g_string_append(str, field_str);
    g_free(field_str);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", margins: ");
  if (self->margins != nullptr) {
    gchar* field_str = hyprbaric_native_layer_shell_margins_to_string(self->margins);
    g_string_append(str, field_str);
    g_free(field_str);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", size: ");
  if (self->size != nullptr) {
    gchar* field_str = hyprbaric_native_layer_shell_size_to_string(self->size);
    g_string_append(str, field_str);
    g_free(field_str);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", exclusive_zone: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->exclusive_zone);
  g_string_append(str, ", auto_exclusive_zone: ");
  g_string_append(str, self->auto_exclusive_zone ? "true" : "false");
  g_string_append(str, ", keyboard_mode: ");
  g_string_append_printf(str, "%d", static_cast<int>(self->keyboard_mode));
  g_string_append(str, ", monitor: ");
  if (self->monitor != nullptr) {
    gchar* field_str = hyprbaric_native_layer_shell_monitor_target_to_string(self->monitor);
    g_string_append(str, field_str);
    g_free(field_str);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _HyprbaricMessageCodec {
  FlStandardMessageCodec parent_instance;

};

G_DEFINE_TYPE(HyprbaricMessageCodec, hyprbaric_message_codec, fl_standard_message_codec_get_type())

const int hyprbaric_native_layer_shell_layer_type_id = 129;
const int hyprbaric_native_layer_shell_keyboard_mode_type_id = 130;
const int hyprbaric_native_layer_shell_bar_edge_type_id = 131;
const int hyprbaric_native_layer_shell_monitor_target_kind_type_id = 132;
const int hyprbaric_native_layer_shell_monitor_target_type_id = 133;
const int hyprbaric_native_layer_shell_monitor_type_id = 134;
const int hyprbaric_native_layer_shell_anchors_type_id = 135;
const int hyprbaric_native_layer_shell_margins_type_id = 136;
const int hyprbaric_native_layer_shell_size_type_id = 137;
const int hyprbaric_native_layer_shell_region_type_id = 138;
const int hyprbaric_native_layer_shell_region_request_type_id = 139;
const int hyprbaric_native_layer_shell_panel_config_type_id = 140;

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_layer(FlStandardMessageCodec* codec, GByteArray* buffer, FlValue* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_layer_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  return fl_standard_message_codec_write_value(codec, buffer, value, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_keyboard_mode(FlStandardMessageCodec* codec, GByteArray* buffer, FlValue* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_keyboard_mode_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  return fl_standard_message_codec_write_value(codec, buffer, value, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_bar_edge(FlStandardMessageCodec* codec, GByteArray* buffer, FlValue* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_bar_edge_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  return fl_standard_message_codec_write_value(codec, buffer, value, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_monitor_target_kind(FlStandardMessageCodec* codec, GByteArray* buffer, FlValue* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_monitor_target_kind_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  return fl_standard_message_codec_write_value(codec, buffer, value, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_monitor_target(FlStandardMessageCodec* codec, GByteArray* buffer, HyprbaricNativeLayerShellMonitorTarget* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_monitor_target_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = hyprbaric_native_layer_shell_monitor_target_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_monitor(FlStandardMessageCodec* codec, GByteArray* buffer, HyprbaricNativeLayerShellMonitor* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_monitor_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = hyprbaric_native_layer_shell_monitor_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_anchors(FlStandardMessageCodec* codec, GByteArray* buffer, HyprbaricNativeLayerShellAnchors* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_anchors_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = hyprbaric_native_layer_shell_anchors_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_margins(FlStandardMessageCodec* codec, GByteArray* buffer, HyprbaricNativeLayerShellMargins* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_margins_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = hyprbaric_native_layer_shell_margins_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_size(FlStandardMessageCodec* codec, GByteArray* buffer, HyprbaricNativeLayerShellSize* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_size_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = hyprbaric_native_layer_shell_size_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_region(FlStandardMessageCodec* codec, GByteArray* buffer, HyprbaricNativeLayerShellRegion* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_region_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = hyprbaric_native_layer_shell_region_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_region_request(FlStandardMessageCodec* codec, GByteArray* buffer, HyprbaricNativeLayerShellRegionRequest* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_region_request_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = hyprbaric_native_layer_shell_region_request_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean hyprbaric_message_codec_write_hyprbaric_native_layer_shell_panel_config(FlStandardMessageCodec* codec, GByteArray* buffer, HyprbaricNativeLayerShellPanelConfig* value, GError** error) {
  uint8_t type = hyprbaric_native_layer_shell_panel_config_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = hyprbaric_native_layer_shell_panel_config_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean hyprbaric_message_codec_write_value(FlStandardMessageCodec* codec, GByteArray* buffer, FlValue* value, GError** error) {
  if (fl_value_get_type(value) == FL_VALUE_TYPE_CUSTOM) {
    switch (fl_value_get_custom_type(value)) {
      case hyprbaric_native_layer_shell_layer_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_layer(codec, buffer, reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value))), error);
      case hyprbaric_native_layer_shell_keyboard_mode_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_keyboard_mode(codec, buffer, reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value))), error);
      case hyprbaric_native_layer_shell_bar_edge_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_bar_edge(codec, buffer, reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value))), error);
      case hyprbaric_native_layer_shell_monitor_target_kind_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_monitor_target_kind(codec, buffer, reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value))), error);
      case hyprbaric_native_layer_shell_monitor_target_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_monitor_target(codec, buffer, HYPRBARIC_NATIVE_LAYER_SHELL_MONITOR_TARGET(fl_value_get_custom_value_object(value)), error);
      case hyprbaric_native_layer_shell_monitor_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_monitor(codec, buffer, HYPRBARIC_NATIVE_LAYER_SHELL_MONITOR(fl_value_get_custom_value_object(value)), error);
      case hyprbaric_native_layer_shell_anchors_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_anchors(codec, buffer, HYPRBARIC_NATIVE_LAYER_SHELL_ANCHORS(fl_value_get_custom_value_object(value)), error);
      case hyprbaric_native_layer_shell_margins_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_margins(codec, buffer, HYPRBARIC_NATIVE_LAYER_SHELL_MARGINS(fl_value_get_custom_value_object(value)), error);
      case hyprbaric_native_layer_shell_size_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_size(codec, buffer, HYPRBARIC_NATIVE_LAYER_SHELL_SIZE(fl_value_get_custom_value_object(value)), error);
      case hyprbaric_native_layer_shell_region_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_region(codec, buffer, HYPRBARIC_NATIVE_LAYER_SHELL_REGION(fl_value_get_custom_value_object(value)), error);
      case hyprbaric_native_layer_shell_region_request_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_region_request(codec, buffer, HYPRBARIC_NATIVE_LAYER_SHELL_REGION_REQUEST(fl_value_get_custom_value_object(value)), error);
      case hyprbaric_native_layer_shell_panel_config_type_id:
        return hyprbaric_message_codec_write_hyprbaric_native_layer_shell_panel_config(codec, buffer, HYPRBARIC_NATIVE_LAYER_SHELL_PANEL_CONFIG(fl_value_get_custom_value_object(value)), error);
    }
  }

  return FL_STANDARD_MESSAGE_CODEC_CLASS(hyprbaric_message_codec_parent_class)->write_value(codec, buffer, value, error);
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_layer(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  return fl_value_new_custom(hyprbaric_native_layer_shell_layer_type_id, fl_standard_message_codec_read_value(codec, buffer, offset, error), (GDestroyNotify)fl_value_unref);
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_keyboard_mode(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  return fl_value_new_custom(hyprbaric_native_layer_shell_keyboard_mode_type_id, fl_standard_message_codec_read_value(codec, buffer, offset, error), (GDestroyNotify)fl_value_unref);
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_bar_edge(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  return fl_value_new_custom(hyprbaric_native_layer_shell_bar_edge_type_id, fl_standard_message_codec_read_value(codec, buffer, offset, error), (GDestroyNotify)fl_value_unref);
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_monitor_target_kind(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  return fl_value_new_custom(hyprbaric_native_layer_shell_monitor_target_kind_type_id, fl_standard_message_codec_read_value(codec, buffer, offset, error), (GDestroyNotify)fl_value_unref);
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_monitor_target(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(HyprbaricNativeLayerShellMonitorTarget) value = hyprbaric_native_layer_shell_monitor_target_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(hyprbaric_native_layer_shell_monitor_target_type_id, G_OBJECT(value));
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_monitor(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(HyprbaricNativeLayerShellMonitor) value = hyprbaric_native_layer_shell_monitor_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(hyprbaric_native_layer_shell_monitor_type_id, G_OBJECT(value));
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_anchors(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(HyprbaricNativeLayerShellAnchors) value = hyprbaric_native_layer_shell_anchors_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(hyprbaric_native_layer_shell_anchors_type_id, G_OBJECT(value));
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_margins(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(HyprbaricNativeLayerShellMargins) value = hyprbaric_native_layer_shell_margins_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(hyprbaric_native_layer_shell_margins_type_id, G_OBJECT(value));
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_size(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(HyprbaricNativeLayerShellSize) value = hyprbaric_native_layer_shell_size_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(hyprbaric_native_layer_shell_size_type_id, G_OBJECT(value));
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_region(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(HyprbaricNativeLayerShellRegion) value = hyprbaric_native_layer_shell_region_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(hyprbaric_native_layer_shell_region_type_id, G_OBJECT(value));
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_region_request(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(HyprbaricNativeLayerShellRegionRequest) value = hyprbaric_native_layer_shell_region_request_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(hyprbaric_native_layer_shell_region_request_type_id, G_OBJECT(value));
}

static FlValue* hyprbaric_message_codec_read_hyprbaric_native_layer_shell_panel_config(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(HyprbaricNativeLayerShellPanelConfig) value = hyprbaric_native_layer_shell_panel_config_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(hyprbaric_native_layer_shell_panel_config_type_id, G_OBJECT(value));
}

static FlValue* hyprbaric_message_codec_read_value_of_type(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, int type, GError** error) {
  switch (type) {
    case hyprbaric_native_layer_shell_layer_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_layer(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_keyboard_mode_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_keyboard_mode(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_bar_edge_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_bar_edge(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_monitor_target_kind_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_monitor_target_kind(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_monitor_target_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_monitor_target(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_monitor_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_monitor(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_anchors_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_anchors(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_margins_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_margins(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_size_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_size(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_region_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_region(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_region_request_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_region_request(codec, buffer, offset, error);
    case hyprbaric_native_layer_shell_panel_config_type_id:
      return hyprbaric_message_codec_read_hyprbaric_native_layer_shell_panel_config(codec, buffer, offset, error);
    default:
      return FL_STANDARD_MESSAGE_CODEC_CLASS(hyprbaric_message_codec_parent_class)->read_value_of_type(codec, buffer, offset, type, error);
  }
}

static void hyprbaric_message_codec_init(HyprbaricMessageCodec* self) {
}

static void hyprbaric_message_codec_class_init(HyprbaricMessageCodecClass* klass) {
  FL_STANDARD_MESSAGE_CODEC_CLASS(klass)->write_value = hyprbaric_message_codec_write_value;
  FL_STANDARD_MESSAGE_CODEC_CLASS(klass)->read_value_of_type = hyprbaric_message_codec_read_value_of_type;
}

static HyprbaricMessageCodec* hyprbaric_message_codec_new() {
  HyprbaricMessageCodec* self = HYPRBARIC_MESSAGE_CODEC(g_object_new(hyprbaric_message_codec_get_type(), nullptr));
  return self;
}

struct _HyprbaricNativeLayerShellHostApiListMonitorsResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiListMonitorsResponse, hyprbaric_native_layer_shell_host_api_list_monitors_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_list_monitors_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiListMonitorsResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_LIST_MONITORS_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_list_monitors_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_list_monitors_response_init(HyprbaricNativeLayerShellHostApiListMonitorsResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_list_monitors_response_class_init(HyprbaricNativeLayerShellHostApiListMonitorsResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_list_monitors_response_dispose;
}

HyprbaricNativeLayerShellHostApiListMonitorsResponse* hyprbaric_native_layer_shell_host_api_list_monitors_response_new(FlValue* return_value) {
  HyprbaricNativeLayerShellHostApiListMonitorsResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_LIST_MONITORS_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_list_monitors_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_ref(return_value));
  return self;
}

HyprbaricNativeLayerShellHostApiListMonitorsResponse* hyprbaric_native_layer_shell_host_api_list_monitors_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiListMonitorsResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_LIST_MONITORS_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_list_monitors_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiCurrentMonitorResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiCurrentMonitorResponse, hyprbaric_native_layer_shell_host_api_current_monitor_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_current_monitor_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiCurrentMonitorResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_CURRENT_MONITOR_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_current_monitor_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_current_monitor_response_init(HyprbaricNativeLayerShellHostApiCurrentMonitorResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_current_monitor_response_class_init(HyprbaricNativeLayerShellHostApiCurrentMonitorResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_current_monitor_response_dispose;
}

HyprbaricNativeLayerShellHostApiCurrentMonitorResponse* hyprbaric_native_layer_shell_host_api_current_monitor_response_new(HyprbaricNativeLayerShellMonitor* return_value) {
  HyprbaricNativeLayerShellHostApiCurrentMonitorResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_CURRENT_MONITOR_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_current_monitor_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, return_value != nullptr ? fl_value_new_custom_object(hyprbaric_native_layer_shell_monitor_type_id, G_OBJECT(return_value)) : fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiCurrentMonitorResponse* hyprbaric_native_layer_shell_host_api_current_monitor_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiCurrentMonitorResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_CURRENT_MONITOR_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_current_monitor_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiConfigurePanelResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiConfigurePanelResponse, hyprbaric_native_layer_shell_host_api_configure_panel_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_configure_panel_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiConfigurePanelResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_CONFIGURE_PANEL_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_configure_panel_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_configure_panel_response_init(HyprbaricNativeLayerShellHostApiConfigurePanelResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_configure_panel_response_class_init(HyprbaricNativeLayerShellHostApiConfigurePanelResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_configure_panel_response_dispose;
}

HyprbaricNativeLayerShellHostApiConfigurePanelResponse* hyprbaric_native_layer_shell_host_api_configure_panel_response_new() {
  HyprbaricNativeLayerShellHostApiConfigurePanelResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_CONFIGURE_PANEL_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_configure_panel_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiConfigurePanelResponse* hyprbaric_native_layer_shell_host_api_configure_panel_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiConfigurePanelResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_CONFIGURE_PANEL_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_configure_panel_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiSetLayerResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiSetLayerResponse, hyprbaric_native_layer_shell_host_api_set_layer_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_set_layer_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiSetLayerResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_LAYER_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_set_layer_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_set_layer_response_init(HyprbaricNativeLayerShellHostApiSetLayerResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_set_layer_response_class_init(HyprbaricNativeLayerShellHostApiSetLayerResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_set_layer_response_dispose;
}

HyprbaricNativeLayerShellHostApiSetLayerResponse* hyprbaric_native_layer_shell_host_api_set_layer_response_new() {
  HyprbaricNativeLayerShellHostApiSetLayerResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_LAYER_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_layer_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiSetLayerResponse* hyprbaric_native_layer_shell_host_api_set_layer_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiSetLayerResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_LAYER_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_layer_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiSetNamespaceResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiSetNamespaceResponse, hyprbaric_native_layer_shell_host_api_set_namespace_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_set_namespace_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiSetNamespaceResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_NAMESPACE_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_set_namespace_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_set_namespace_response_init(HyprbaricNativeLayerShellHostApiSetNamespaceResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_set_namespace_response_class_init(HyprbaricNativeLayerShellHostApiSetNamespaceResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_set_namespace_response_dispose;
}

HyprbaricNativeLayerShellHostApiSetNamespaceResponse* hyprbaric_native_layer_shell_host_api_set_namespace_response_new() {
  HyprbaricNativeLayerShellHostApiSetNamespaceResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_NAMESPACE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_namespace_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiSetNamespaceResponse* hyprbaric_native_layer_shell_host_api_set_namespace_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiSetNamespaceResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_NAMESPACE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_namespace_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiSetAnchorsResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiSetAnchorsResponse, hyprbaric_native_layer_shell_host_api_set_anchors_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_set_anchors_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiSetAnchorsResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_ANCHORS_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_set_anchors_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_set_anchors_response_init(HyprbaricNativeLayerShellHostApiSetAnchorsResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_set_anchors_response_class_init(HyprbaricNativeLayerShellHostApiSetAnchorsResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_set_anchors_response_dispose;
}

HyprbaricNativeLayerShellHostApiSetAnchorsResponse* hyprbaric_native_layer_shell_host_api_set_anchors_response_new() {
  HyprbaricNativeLayerShellHostApiSetAnchorsResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_ANCHORS_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_anchors_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiSetAnchorsResponse* hyprbaric_native_layer_shell_host_api_set_anchors_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiSetAnchorsResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_ANCHORS_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_anchors_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiSetMarginsResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiSetMarginsResponse, hyprbaric_native_layer_shell_host_api_set_margins_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_set_margins_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiSetMarginsResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_MARGINS_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_set_margins_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_set_margins_response_init(HyprbaricNativeLayerShellHostApiSetMarginsResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_set_margins_response_class_init(HyprbaricNativeLayerShellHostApiSetMarginsResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_set_margins_response_dispose;
}

HyprbaricNativeLayerShellHostApiSetMarginsResponse* hyprbaric_native_layer_shell_host_api_set_margins_response_new() {
  HyprbaricNativeLayerShellHostApiSetMarginsResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_MARGINS_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_margins_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiSetMarginsResponse* hyprbaric_native_layer_shell_host_api_set_margins_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiSetMarginsResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_MARGINS_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_margins_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponse, hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_EXCLUSIVE_ZONE_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response_init(HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response_class_init(HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response_dispose;
}

HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponse* hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response_new() {
  HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_EXCLUSIVE_ZONE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponse* hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_EXCLUSIVE_ZONE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_exclusive_zone_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponse, hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_AUTO_EXCLUSIVE_ZONE_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response_init(HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response_class_init(HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response_dispose;
}

HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponse* hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response_new() {
  HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_AUTO_EXCLUSIVE_ZONE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponse* hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_AUTO_EXCLUSIVE_ZONE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiSetKeyboardModeResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiSetKeyboardModeResponse, hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiSetKeyboardModeResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_KEYBOARD_MODE_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response_init(HyprbaricNativeLayerShellHostApiSetKeyboardModeResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response_class_init(HyprbaricNativeLayerShellHostApiSetKeyboardModeResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response_dispose;
}

HyprbaricNativeLayerShellHostApiSetKeyboardModeResponse* hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response_new() {
  HyprbaricNativeLayerShellHostApiSetKeyboardModeResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_KEYBOARD_MODE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiSetKeyboardModeResponse* hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiSetKeyboardModeResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_KEYBOARD_MODE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_keyboard_mode_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiSetSizeResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiSetSizeResponse, hyprbaric_native_layer_shell_host_api_set_size_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_set_size_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiSetSizeResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_SIZE_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_set_size_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_set_size_response_init(HyprbaricNativeLayerShellHostApiSetSizeResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_set_size_response_class_init(HyprbaricNativeLayerShellHostApiSetSizeResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_set_size_response_dispose;
}

HyprbaricNativeLayerShellHostApiSetSizeResponse* hyprbaric_native_layer_shell_host_api_set_size_response_new() {
  HyprbaricNativeLayerShellHostApiSetSizeResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_SIZE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_size_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiSetSizeResponse* hyprbaric_native_layer_shell_host_api_set_size_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiSetSizeResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_SIZE_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_size_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApiSetRegionResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApiSetRegionResponse, hyprbaric_native_layer_shell_host_api_set_region_response, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_set_region_response_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApiSetRegionResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_REGION_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_set_region_response_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_set_region_response_init(HyprbaricNativeLayerShellHostApiSetRegionResponse* self) {
}

static void hyprbaric_native_layer_shell_host_api_set_region_response_class_init(HyprbaricNativeLayerShellHostApiSetRegionResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_set_region_response_dispose;
}

HyprbaricNativeLayerShellHostApiSetRegionResponse* hyprbaric_native_layer_shell_host_api_set_region_response_new() {
  HyprbaricNativeLayerShellHostApiSetRegionResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_REGION_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_region_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_null());
  return self;
}

HyprbaricNativeLayerShellHostApiSetRegionResponse* hyprbaric_native_layer_shell_host_api_set_region_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  HyprbaricNativeLayerShellHostApiSetRegionResponse* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API_SET_REGION_RESPONSE(g_object_new(hyprbaric_native_layer_shell_host_api_set_region_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _HyprbaricNativeLayerShellHostApi {
  GObject parent_instance;

  const HyprbaricNativeLayerShellHostApiVTable* vtable;
  gpointer user_data;
  GDestroyNotify user_data_free_func;
};

G_DEFINE_TYPE(HyprbaricNativeLayerShellHostApi, hyprbaric_native_layer_shell_host_api, G_TYPE_OBJECT)

static void hyprbaric_native_layer_shell_host_api_dispose(GObject* object) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(object);
  if (self->user_data != nullptr) {
    self->user_data_free_func(self->user_data);
  }
  self->user_data = nullptr;
  G_OBJECT_CLASS(hyprbaric_native_layer_shell_host_api_parent_class)->dispose(object);
}

static void hyprbaric_native_layer_shell_host_api_init(HyprbaricNativeLayerShellHostApi* self) {
}

static void hyprbaric_native_layer_shell_host_api_class_init(HyprbaricNativeLayerShellHostApiClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = hyprbaric_native_layer_shell_host_api_dispose;
}

static HyprbaricNativeLayerShellHostApi* hyprbaric_native_layer_shell_host_api_new(const HyprbaricNativeLayerShellHostApiVTable* vtable, gpointer user_data, GDestroyNotify user_data_free_func) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(g_object_new(hyprbaric_native_layer_shell_host_api_get_type(), nullptr));
  self->vtable = vtable;
  self->user_data = user_data;
  self->user_data_free_func = user_data_free_func;
  return self;
}

static void hyprbaric_native_layer_shell_host_api_list_monitors_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->list_monitors == nullptr) {
    return;
  }

  g_autoptr(HyprbaricNativeLayerShellHostApiListMonitorsResponse) response = self->vtable->list_monitors(self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "listMonitors");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "listMonitors", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_current_monitor_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->current_monitor == nullptr) {
    return;
  }

  g_autoptr(HyprbaricNativeLayerShellHostApiCurrentMonitorResponse) response = self->vtable->current_monitor(self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "currentMonitor");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "currentMonitor", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_configure_panel_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->configure_panel == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  HyprbaricNativeLayerShellPanelConfig* config = HYPRBARIC_NATIVE_LAYER_SHELL_PANEL_CONFIG(fl_value_get_custom_value_object(value0));
  g_autoptr(HyprbaricNativeLayerShellHostApiConfigurePanelResponse) response = self->vtable->configure_panel(config, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "configurePanel");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "configurePanel", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_set_layer_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_layer == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  HyprbaricNativeLayerShellLayer layer = static_cast<HyprbaricNativeLayerShellLayer>(fl_value_get_int(reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value0)))));
  g_autoptr(HyprbaricNativeLayerShellHostApiSetLayerResponse) response = self->vtable->set_layer(layer, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "setLayer");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "setLayer", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_set_namespace_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_namespace == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  const gchar* app_namespace = fl_value_get_string(value0);
  g_autoptr(HyprbaricNativeLayerShellHostApiSetNamespaceResponse) response = self->vtable->set_namespace(app_namespace, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "setNamespace");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "setNamespace", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_set_anchors_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_anchors == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  HyprbaricNativeLayerShellAnchors* anchors = HYPRBARIC_NATIVE_LAYER_SHELL_ANCHORS(fl_value_get_custom_value_object(value0));
  g_autoptr(HyprbaricNativeLayerShellHostApiSetAnchorsResponse) response = self->vtable->set_anchors(anchors, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "setAnchors");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "setAnchors", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_set_margins_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_margins == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  HyprbaricNativeLayerShellMargins* margins = HYPRBARIC_NATIVE_LAYER_SHELL_MARGINS(fl_value_get_custom_value_object(value0));
  g_autoptr(HyprbaricNativeLayerShellHostApiSetMarginsResponse) response = self->vtable->set_margins(margins, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "setMargins");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "setMargins", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_set_exclusive_zone_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_exclusive_zone == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  int64_t zone = fl_value_get_int(value0);
  g_autoptr(HyprbaricNativeLayerShellHostApiSetExclusiveZoneResponse) response = self->vtable->set_exclusive_zone(zone, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "setExclusiveZone");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "setExclusiveZone", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_auto_exclusive_zone == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  gboolean enabled = fl_value_get_bool(value0);
  g_autoptr(HyprbaricNativeLayerShellHostApiSetAutoExclusiveZoneResponse) response = self->vtable->set_auto_exclusive_zone(enabled, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "setAutoExclusiveZone");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "setAutoExclusiveZone", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_set_keyboard_mode_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_keyboard_mode == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  HyprbaricNativeLayerShellKeyboardMode mode = static_cast<HyprbaricNativeLayerShellKeyboardMode>(fl_value_get_int(reinterpret_cast<FlValue*>(const_cast<gpointer>(fl_value_get_custom_value(value0)))));
  g_autoptr(HyprbaricNativeLayerShellHostApiSetKeyboardModeResponse) response = self->vtable->set_keyboard_mode(mode, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "setKeyboardMode");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "setKeyboardMode", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_set_size_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_size == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  HyprbaricNativeLayerShellSize* size = HYPRBARIC_NATIVE_LAYER_SHELL_SIZE(fl_value_get_custom_value_object(value0));
  g_autoptr(HyprbaricNativeLayerShellHostApiSetSizeResponse) response = self->vtable->set_size(size, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "setSize");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "setSize", error->message);
  }
}

static void hyprbaric_native_layer_shell_host_api_set_region_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  HyprbaricNativeLayerShellHostApi* self = HYPRBARIC_NATIVE_LAYER_SHELL_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_region == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  HyprbaricNativeLayerShellRegionRequest* request = HYPRBARIC_NATIVE_LAYER_SHELL_REGION_REQUEST(fl_value_get_custom_value_object(value0));
  g_autoptr(HyprbaricNativeLayerShellHostApiSetRegionResponse) response = self->vtable->set_region(request, self->user_data);
  if (response == nullptr) {
    g_warning("No response returned to %s.%s", "NativeLayerShellHostApi", "setRegion");
    return;
  }

  g_autoptr(GError) error = NULL;
  if (!fl_basic_message_channel_respond(channel, response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "NativeLayerShellHostApi", "setRegion", error->message);
  }
}

void hyprbaric_native_layer_shell_host_api_set_method_handlers(FlBinaryMessenger* messenger, const gchar* suffix, const HyprbaricNativeLayerShellHostApiVTable* vtable, gpointer user_data, GDestroyNotify user_data_free_func) {
  g_autofree gchar* dot_suffix = suffix != nullptr ? g_strdup_printf(".%s", suffix) : g_strdup("");
  g_autoptr(HyprbaricNativeLayerShellHostApi) api_data = hyprbaric_native_layer_shell_host_api_new(vtable, user_data, user_data_free_func);

  g_autoptr(HyprbaricMessageCodec) codec = hyprbaric_message_codec_new();
  g_autofree gchar* list_monitors_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.listMonitors%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) list_monitors_channel = fl_basic_message_channel_new(messenger, list_monitors_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(list_monitors_channel, hyprbaric_native_layer_shell_host_api_list_monitors_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* current_monitor_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.currentMonitor%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) current_monitor_channel = fl_basic_message_channel_new(messenger, current_monitor_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(current_monitor_channel, hyprbaric_native_layer_shell_host_api_current_monitor_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* configure_panel_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.configurePanel%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) configure_panel_channel = fl_basic_message_channel_new(messenger, configure_panel_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(configure_panel_channel, hyprbaric_native_layer_shell_host_api_configure_panel_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_layer_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setLayer%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_layer_channel = fl_basic_message_channel_new(messenger, set_layer_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_layer_channel, hyprbaric_native_layer_shell_host_api_set_layer_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_namespace_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setNamespace%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_namespace_channel = fl_basic_message_channel_new(messenger, set_namespace_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_namespace_channel, hyprbaric_native_layer_shell_host_api_set_namespace_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_anchors_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setAnchors%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_anchors_channel = fl_basic_message_channel_new(messenger, set_anchors_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_anchors_channel, hyprbaric_native_layer_shell_host_api_set_anchors_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_margins_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setMargins%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_margins_channel = fl_basic_message_channel_new(messenger, set_margins_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_margins_channel, hyprbaric_native_layer_shell_host_api_set_margins_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_exclusive_zone_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setExclusiveZone%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_exclusive_zone_channel = fl_basic_message_channel_new(messenger, set_exclusive_zone_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_exclusive_zone_channel, hyprbaric_native_layer_shell_host_api_set_exclusive_zone_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_auto_exclusive_zone_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setAutoExclusiveZone%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_auto_exclusive_zone_channel = fl_basic_message_channel_new(messenger, set_auto_exclusive_zone_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_auto_exclusive_zone_channel, hyprbaric_native_layer_shell_host_api_set_auto_exclusive_zone_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_keyboard_mode_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setKeyboardMode%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_keyboard_mode_channel = fl_basic_message_channel_new(messenger, set_keyboard_mode_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_keyboard_mode_channel, hyprbaric_native_layer_shell_host_api_set_keyboard_mode_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_size_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setSize%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_size_channel = fl_basic_message_channel_new(messenger, set_size_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_size_channel, hyprbaric_native_layer_shell_host_api_set_size_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_region_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setRegion%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_region_channel = fl_basic_message_channel_new(messenger, set_region_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_region_channel, hyprbaric_native_layer_shell_host_api_set_region_cb, g_object_ref(api_data), g_object_unref);
}

void hyprbaric_native_layer_shell_host_api_clear_method_handlers(FlBinaryMessenger* messenger, const gchar* suffix) {
  g_autofree gchar* dot_suffix = suffix != nullptr ? g_strdup_printf(".%s", suffix) : g_strdup("");

  g_autoptr(HyprbaricMessageCodec) codec = hyprbaric_message_codec_new();
  g_autofree gchar* list_monitors_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.listMonitors%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) list_monitors_channel = fl_basic_message_channel_new(messenger, list_monitors_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(list_monitors_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* current_monitor_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.currentMonitor%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) current_monitor_channel = fl_basic_message_channel_new(messenger, current_monitor_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(current_monitor_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* configure_panel_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.configurePanel%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) configure_panel_channel = fl_basic_message_channel_new(messenger, configure_panel_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(configure_panel_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_layer_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setLayer%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_layer_channel = fl_basic_message_channel_new(messenger, set_layer_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_layer_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_namespace_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setNamespace%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_namespace_channel = fl_basic_message_channel_new(messenger, set_namespace_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_namespace_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_anchors_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setAnchors%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_anchors_channel = fl_basic_message_channel_new(messenger, set_anchors_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_anchors_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_margins_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setMargins%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_margins_channel = fl_basic_message_channel_new(messenger, set_margins_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_margins_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_exclusive_zone_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setExclusiveZone%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_exclusive_zone_channel = fl_basic_message_channel_new(messenger, set_exclusive_zone_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_exclusive_zone_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_auto_exclusive_zone_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setAutoExclusiveZone%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_auto_exclusive_zone_channel = fl_basic_message_channel_new(messenger, set_auto_exclusive_zone_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_auto_exclusive_zone_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_keyboard_mode_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setKeyboardMode%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_keyboard_mode_channel = fl_basic_message_channel_new(messenger, set_keyboard_mode_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_keyboard_mode_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_size_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setSize%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_size_channel = fl_basic_message_channel_new(messenger, set_size_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_size_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_region_channel_name = g_strdup_printf("dev.flutter.pigeon.hyprbaric.NativeLayerShellHostApi.setRegion%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_region_channel = fl_basic_message_channel_new(messenger, set_region_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_region_channel, nullptr, nullptr, nullptr);
}
